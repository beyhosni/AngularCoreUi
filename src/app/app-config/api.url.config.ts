
const BASE = 'http://localhost';
const SEP = ':';
const PORT_API = '8088';
const PORT_SEC = '8080';

export const API_URLS  = {
  USER_URL: BASE + SEP + PORT_SEC + '/login',
  USER_URL_REGISTER: BASE + SEP + PORT_SEC + '/register'
};

